# Fixtures

The integration tests execute small explicit system commands and store reports
in temporary paths. The repository contains no benchmark binaries, generated
reports, or private host metadata.
